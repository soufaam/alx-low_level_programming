#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{       char mychar[] = "\"Programming is like building a multilingual puzzle";
	puts(mychar);
    return (0);
}

